name: dependabot

on:
  pull_request:
    branches:
      - main
      - dependabot/**

jobs:
  automerge:
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: write

    steps:
      - uses: fountainhead/action-wait-for-check@v1.0.0
        id: wait-for-lint-js
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: lint-javascript
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: fountainhead/action-wait-for-check@v1.0.0
        if: steps.wait-for-lint-js.outputs.conclusion == 'success'
        id: wait-for-lint-ts
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: lint-typescript
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: fountainhead/action-wait-for-check@v1.0.0
        if: steps.wait-for-lint-ts.outputs.conclusion == 'success'
        id: wait-for-unit
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: unit
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: fastify/github-action-merge-dependabot@v3
        if: steps.wait-for-unit.outputs.conclusion == 'success'
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          merge-method: squash
          target: any
