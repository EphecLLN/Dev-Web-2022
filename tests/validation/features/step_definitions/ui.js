/* eslint-disable prefer-arrow-callback */
const { Given, When, Then } = require("@cucumber/cucumber")

Given("an authentication form is displayed", function() {
  return "pending"
})

// FIXME: Not UI
Given("the game has started", () => {
  return "pending"
})

// FIXME: Not UI
When("the player sends a message", function() {
  return "pending"
})

// FIXME: Not UI
When("a message is sent", function () {
  return "pending"
})

When("the player enters their credentials", function() {
  return "pending"
})

Then("the player cannot send chat messages", function() {
  return "pending"
})

Then("the player sees the message they sent", function() {
  return "pending"
})

Then("all other players see the message the player sent", function() {
  return "pending"
})

Then("the player's name is next to the message they sent", function() {
  return "pending"
})

Then("the displays scrolls to the latest message", function() {
  return "pending"
})
