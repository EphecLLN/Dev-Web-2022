/* eslint-disable prefer-arrow-callback */
const { Given, When, Then } = require("@cucumber/cucumber")

Given("{int} player accounts exist", function(_int) {
  return "pending"
})

Given("the player is logged in", function() {
  return "pending"
})

Given("at least {int} other players are logged in", function(_int) {
  return "pending"
})

When("the player is not logged in", function() {
  return "pending"
})

Then("the player is logged into a game", function() {
  return "pending"
})

